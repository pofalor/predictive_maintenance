import streamlit as st
import pandas as pd
import matplotlib.pyplot as plt


def presentation_page():
    st.title("Презентация проекта")

    st.markdown("""
    ## Прогнозирование отказов оборудования

    **Цель проекта:** Разработка системы предиктивного обслуживания промышленного оборудования 
    с использованием машинного обучения.

    ### Основные этапы работы:
    1. **Загрузка данных** - Использование датасета AI4I 2020 Predictive Maintenance
    2. **Предобработка** - Очистка данных, преобразование признаков, масштабирование
    3. **Обучение модели** - Random Forest для бинарной классификации
    4. **Оценка качества** - Метрики accuracy, confusion matrix, feature importance
    5. **Развертывание** - Streamlit-приложение с интерактивным интерфейсом

    ### Ключевые характеристики:
    - **Точность модели:** >96%
    - **Основные признаки:** Крутящий момент, скорость вращения, износ инструмента
    - **Технологический стек:**
        - Python 3.10
        - Scikit-learn, Pandas, NumPy
        - Streamlit, Matplotlib, Seaborn

    ### Пример данных:
    """)

    # Пример данных
    sample_data = pd.DataFrame({
        'Параметр': ['Тип продукта', 'Температура воздуха', 'Температура процесса',
                     'Скорость вращения', 'Крутящий момент', 'Износ инструмента'],
        'Единицы измерения': ['L/M/H', 'K', 'K', 'rpm', 'Nm', 'min'],
        'Диапазон значений': ['Категория', '295-305', '305-315', '1200-2900', '5-80', '0-250']
    })
    st.table(sample_data)

    st.markdown("""
    ### Распределение целевой переменной:
    """)

    # Визуализация распределения
    fig, ax = plt.subplots(figsize=(8, 4))
    ax.pie([950, 50], labels=['Норма (95%)', 'Отказ (5%)'],
           autopct='%1.1f%%', colors=['#4CAF50', '#F44336'])
    ax.set_title("Соотношение норма/отказ в датасете")
    st.pyplot(fig)

    st.markdown("""
    ## Инструкция по использованию:
    1. Перейдите на вкладку **"Анализ и модель"**
    2. Загрузите CSV-файл с данными оборудования
    3. Дождитесь обучения модели
    4. Используйте форму для прогнозирования состояния оборудования
    """)